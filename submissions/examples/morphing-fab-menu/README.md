# Morphing FAB Menu

A clean floating action button (FAB) that expands and morphs into a responsive action bar on interaction.

## Features
- Complex scaling calculations using pure CSS transitions
- Dynamic width/height animations
- Staggered items transitions